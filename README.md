# Search

一个简单的搜索页面

## 指令（输入 `/命令` 后按空格执行）
- 搜索引擎：`/g` Google、`/b` 百度、`/bi` Bing、`/gh` GitHub、`/v` 哔哩哔哩、`/z` 知乎、`/y` YouTube
- 主题：`/dark` 深色、`/light` 浅色

## 快捷键
- Tab：接受计算预览
- Esc：清空输入
- ↑ / ↓：浏览历史
- Enter：提交；输入 URL/域名则直接跳转
